export { buildAllDemoData } from "@/mocks/builder"

export type { DemoData } from "@/mocks/builder"

export { resetDemoData } from "@/mocks/reset"

export {
  LOGIN_DEMO_USER_IDS,
  isLoginDemoUser,
} from "@/mocks/constants"

export { MOCK_USERS, findMockUser, homePathForRole } from "@/lib/mock-users"
export type { MockUser, Role } from "@/lib/mock-users"

export { PRODUCTS, findProduct } from "@/lib/products"
export type { Product, ProductForm } from "@/lib/products"

export { INITIAL_ACCOUNT_LIMITS } from "@/mocks/accounts"
